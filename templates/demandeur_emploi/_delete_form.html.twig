<form method="post" action="{{ path('demandeur_emploi_delete', {'id': demandeur_emploi.id}) }}" class="btn btn-danger" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demandeur_emploi.id) }}">
    <button class="btn">Delete</button>
</form>
