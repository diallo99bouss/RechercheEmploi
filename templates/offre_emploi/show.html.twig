{% extends 'base.html.twig' %}

{% block title %}OffreEmploi{% endblock %}

{% block body %}
    <h1>OffreEmploi</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offre_emploi.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ offre_emploi.libelle }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ offre_emploi.categorie }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ offre_emploi.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('offre_emploi_index') }}" class="btn btn-primary">back to list</a>

    <a href="{{ path('offre_emploi_edit', {'id': offre_emploi.id}) }}" class="btn btn-secondary">edit</a>

    {{ include('offre_emploi/_delete_form.html.twig') }}
{% endblock %}
