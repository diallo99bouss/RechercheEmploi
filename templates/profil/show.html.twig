{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}
    <h1>Profil</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ profil.id }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ profil.adresse }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ profil.email }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ profil.telephone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('profil_index') }}" class="btn btn-primary">back to list</a>

    <a href="{{ path('profil_edit', {'id': profil.id}) }}" class="btn btn-secondary">edit</a>

    {{ include('profil/_delete_form.html.twig') }}
{% endblock %}
